- name: upgrade/update apt based hosts
  hosts: all
  become: yes
  vars:
    ansible_user: olivier
  tasks:
  - include_tasks: tasks/apt-upgrade-tasks.yml
    tags: always

  - include_tasks: tasks/vim-task.yml
    tags: always

  - include_tasks: tasks/rust-task.yml
    tags: always

  - include_tasks: tasks/rust-cargo-task.yml
    tags: always

  - include_tasks: tasks/helm-task.yml
    tags: always

  - include_tasks: tasks/i2c-task.yml
    tags: always

  - include_tasks: tasks/yubikey-task.yml
    tags: always

  - include_tasks: tasks/step-cli-task.yml
    tags: always

  - include_tasks: tasks/tools-task.yml
    tags: always

  - include_tasks: tasks/certbot-task.yml
    tags: always

  - include_tasks: tasks/info-display-task.yml
    tags: always

  - include_tasks: tasks/longhorn-prep-task.yml
    tags: always